server.py
    server endpoints, calls scan for bt devices and CRUD ops on db

bt.py
    bluetooth specific functions

empldb.py
    DB class that uses sqlite3 api for CRUD ops on data

Required Unix packages on pi device (for pybluez to work):
    sudo apt-get install libbluetooth-dev
    sudo apt-get install python-dev
    sudo apt-get install python-bluez
    sudo apt install bluetooth pi-bluetooth bluez

pip install -r requirements.txt prior to running
    or individually install

straightforward run calls:
    python server.py